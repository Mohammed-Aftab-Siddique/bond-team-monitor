name: custom:bond-team-monitor
version: 1.0.0

minDynatraceVersion: "1.285"

author:
  name: "Mohammed Aftab Siddique"

python:
  runtime:
    module: bond_team_monitor
    version:
      min: "3.10"
      max: "3.10"

  activation:
    remote:
      path: activationSchema.json
    local:
      path: activationSchema.json
